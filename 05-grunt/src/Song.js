/**
 * @class Song.
 * Song representation (no implementation yet).
 */
function Song() {
}
/**
 * persist favorite status for the Song.
 * @param {boolean} if the song isfavorite.
 */
Song.prototype.persistFavoriteStatus = function(value) {
  // something complicated
  throw new Error("not yet implemented");
};